本次实验完成了多线程相关机制和同步原语的实现，包括线程创建、等待、退出，以及 mutex、semaphore、condvar 等同步机制。同时实现了死锁检测开关系统调用，并在 mutex_lock 和 semaphore_down 中接入检测逻辑，当检测到可能死锁时拒绝操作并返回指定错误码。

在多线程实现中，0 号线程通常被视为主线程。当主线程退出时，整个进程都应退出，因此需要结束该进程中的所有线程，并回收相关资源。

需要回收的资源主要包括：
所有线程的 TaskControlBlock
每个线程的内核栈
每个线程的用户态资源
线程 id / tid 资源
调度队列中的引用
同步原语等待队列中的引用
进程级资源

这两种实现的核心区别在于：解锁时是否立即将锁状态设为未锁定，以及被唤醒线程是否需要重新竞争锁

`Mutex2` 在 `lock` 中只判断一次锁状态。
如果锁被占用，线程进入等待队列并阻塞；当它被唤醒后，函数直接返回，不会再次检查锁状态
在 `unlock` 中，如果等待队列中有线程，它只唤醒一个线程，但不会把 `locked` 设置为 `false`。这相当于把锁直接交给被唤醒的线程。只有当等待队列为空时，才将 `locked` 设置为 `false`。

在完成本次实验的过程（含此前学习的过程）中，我曾分别与 以下各位 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：

gpt

此外，我也参考了 以下资料 ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：

https://learningos.cn/rCore-Tutorial-Guide/honorcode.html

3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。


